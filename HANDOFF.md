# 三国覇王戦記 武将DB＋編成シミュレーター — Claude Code 引き継ぎ仕様書

> このファイルは、ここまで Claude（チャット側）で検討した内容を Claude Code に
> 引き継ぐためのハンドオフ資料です。Claude Code はまずこの `HANDOFF.md` を
> 読み、続けて同梱の `generals_sample.yaml`（データ構造サンプル）を読んでから
> 作業を始めてください。

---

## 0. このプロジェクトの目的

スマホゲーム **「三国覇王戦記～乱世の系譜～」（Six Waves Inc. 配信版）** の
武将データを構造化し、**軍団スキル上限・集結スキル上限を考慮した編成**を
組めるツールを作る。

最終成果物は次の2つ:

1. **武将データベース（YAML）** — 橙ランク＋玄武将（覚醒版）を収録
2. **編成シミュレーター（HTML 単一ファイル / JS 計算ツール）** — プレイヤーが
   各武将の育成段階を入力すると、スキル上限を考慮した実効値を集計・可視化する

成果物はゆくゆく AI（Claude 等）に読み込ませて編成提案させる用途も想定。
そのためデータは「AIが上限ルールを判断できる」構造にしてある。

---

## 1. 対象ゲームの確定情報（同名タイトル注意）

| 項目 | 内容 |
|---|---|
| 正式名称 | 三国覇王戦記～乱世の系譜～ |
| 配信元 | Six Waves Inc.（6waves株式会社） |
| 配信日 | 2018年3月27日 |
| ジャンル | 縦型三国ストラテジー |
| 公式サイト | https://haou.6waves.jp/ |

**別ゲームと混同しないこと**: 「三國志 覇道」（コーエー）、「三國志 真戦」、
「新三國志」、「三国大戦スマッシュ」等は全て別タイトル。データ収集時に
URLのドメインがこのゲームのものか必ず確認する。

主な情報源（データ拡充時に参照）:
- 公認Wiki: https://sangoku-haou.gamer-wiki.com/
- 上級者ブログ（現環境・中国版先行情報）: https://www.haou-no-waza.com/
- 武将一覧: https://sangoku-haou.gamer-wiki.com/character-list

---

## 2. ゲーム仕様メモ（実装で必要な前提知識）

### 兵科
歩兵・騎兵・弓兵・戦車（角陣専用）・万能。

### 編成構造
- 主陣 = 最大5部隊 × 3将 = 15将
- 角陣（副陣）= 1部隊 × 3将 = 3将（戦車専用）
- 集結 = 複数プレイヤーの軍団が合流して1目標を攻撃

### スキルの「効果スコープ」（最重要概念）
スキルがどの範囲に効くかで、上限の扱いが変わる。

| scope | 意味 | 上限の扱い |
|---|---|---|
| `self` | 武将自身/所属部隊のみ | 上限の概念なし |
| `legion` | 軍団全体に作用 | **軍団上限**の累加対象 |
| `rally` | 集結全体に作用 | **集結上限**の累加対象 |
| `array` | 角陣（戦車副陣）に作用 | 別系統 |

### スキル上限ルール（cap_rules）
同種スキルを重ねると、合算値が上限で頭打ちになる。判明分:

| ルール | 上限 | 例 |
|---|---|---|
| 集結・敵攻撃ダウン系 | 合算 -50% | 関羽「武聖」 |
| 集結・抑制操作系 | 80% | 張遼「虎威」 |
| 集結・敵シールド減少 | 累計 -1000万pt | 馬超 |
| 集結・全体攻撃アップ系 | +50% | 張寧姫「克敵攻虎」 |
| 集結・連撃アップ系 | +60% | 張寧姫「疾風虎勁」 |
| 軍団バフ全般 | **未確認（unknown）** | 突撃・恐弦 等 |
| 上限なし系（積み得） | なし | 黄忠「神射」、賈逵「盾防」 |

**注意**: 軍団バフの累加上限は公開情報で確認できていない。データ上は
`unknown` とし、シミュレーターは「上限未確認」と明示表示する。
無理に数値を捏造しないこと（ハルシネーション厳禁）。

---

## 3. 育成段階モデル（この設計の肝）

同じ武将でも、プレイヤーの育成状況でスキル倍率・習得スキルが変わる。
**育成段階を独立した4軸**として持つ。プレイヤーごとに組み合わせが異なる
（例: 「突破4・装備2・Lv突破あり・将印1」が普通に成立する）。

| 軸 | キー | 値域 | 影響 |
|---|---|---|---|
| 武将突破（覚醒進度） | `breakthrough` | 0〜4 | 進度4で「玄武将」化、覚醒スキル解放。スキル倍率に最も影響 |
| 装備段階 | `equipment` | 0〜4 | 兵士属性中心。スキル倍率に効く場合あり |
| Lv上限突破 | `level_break` | false / true | 率兵数・基礎ステータス |
| 将印 | `seal` | 0〜5 | 追加強化枠。将印固有効果あり |

### スキル値の持ち方
各スキルの `value` は **段階別の実数テーブル**（配列）で持つ:

```yaml
value:
  by_breakthrough: [null, null, null, null, 20]  # 突破0〜4の実数
  by_equipment:    [0, null, null, null, null]   # 装備0〜4による加算
  confidence: "low"
```

- 判明している段だけ実数を入れ、**不明な段は `null`**
- `unlocked_at`（覚醒スキルの解放突破段）でスキルの存在可否を表現
- `seal_bonus.by_seal`（0〜5の配列）で将印効果

---

## 4. 重要な意思決定事項（ユーザーと合意済み / 未決）

### 合意済み
- 収録範囲: **橙ランク ＋ 玄武将（覚醒版）**
- 成果物: **YAML武将DB ＋ HTML編成シミュレーター**
- 上限の扱い: **上限値をデータに持たせ、AI／ツールが判断**
- 段階管理: **突破・装備・Lv突破・将印を独立4軸で細かく管理**
- スキル値: **実数で持つ。ただしユーザーは細かい段階別数値を持っていない**
  → 構造は実数対応にし、判明分のみ埋め、不明は `null`＋`confidence:unknown`

### 未決（Claude Code 着手時にユーザーへ確認すること）
1. **`by_breakthrough` 配列の運用方針**
   - 案A: 5要素配列のまま維持。当面は突破4だけ埋め、途中段階は null
     （構造は完全だが当面スカスカ）
   - 案B: いったん「突破4の値＋unlocked_at」だけに簡略化し、装備・将印は
     係数モデルで簡易化（すぐ実用、後で拡張要）
   - ユーザーの希望は「細かく把握したい」なので **案A 寄り**だが、
     最初に動くものを見せたいなら案Bから入る選択もあり。要確認。
2. 将印（seal）の段数が 0〜5 で正しいか（ゲーム内仕様の確認が必要）
3. ステータス数値（統率・武力等）を収録するか、スキル特化にするか
   （攻略サイトに正確な数値が少ないため）

---

## 5. データ構造サンプル

同梱の **`generals_sample.yaml`** が現時点の構造サンプル（v0.2）。
張遼・于禁の2体で、段階対応構造・スキルスコープ・上限ルールを表現している。
このファイルの構造をベースに全武将へ展開する。

YAMLの妥当性は Python の `yaml.safe_load` で検証済み。

---

## 6. 実装タスク（推奨順）

### フェーズ1: データ構造の確定
- [ ] `generals_sample.yaml` の構造をユーザーと最終確認
- [ ] 上記「未決事項」をユーザーに確認して方針確定
- [ ] スキーマを `schema.md` として明文化（各フィールドの型・必須/任意）

### フェーズ2: 武将データの収集・拡充
- [ ] 橙ランク武将一覧を gamer-wiki / haou-no-waza から収集（約60体）
- [ ] 玄武将（覚醒版）一覧を収集（20体以上）
- [ ] 各武将のスキル名・効果・scope・cap_rule を登録
- [ ] 判明している倍率（主に突破4時点）を `by_breakthrough[4]` に記入
- [ ] 各値に `confidence`（high/medium/low/unknown）を付与
- [ ] 出典URLを各武将の `sources` に記録
- [ ] 全データを `generals.yaml` に統合し、`yaml.safe_load` で検証

### フェーズ3: 編成シミュレーター（HTML単一ファイル）
- [ ] YAML を読み込む（JSビルド時に JSON 変換 or fetch）
- [ ] 武将選択UI（兵科・レアリティでフィルタ）
- [ ] 武将ごとに育成段階を入力するUI
      （突破 0-4 / 装備 0-4 / Lv突破 ON-OFF / 将印 0-5）
- [ ] 段階に応じたスキル解決ロジック:
      - `unlocked_at > breakthrough` ならスキル非表示（未解放）
      - `value.by_breakthrough[breakthrough]` が null なら「要確認」表示
      - 判明値があれば 突破段＋装備段＋将印段 を合算して実効値に
- [ ] スコープ別の集計:
      - `legion` スキルを軍団内で合算
      - `rally` スキルを集結内で合算
- [ ] 上限判定:
      - `cap_rules` を適用し、超過分を「オーバーフロー（無効）」として可視化
      - `unknown` 上限は「単純加算の参考値＋上限未確認」と注記
- [ ] confidence:low の値を編成根拠に使う箇所に「要ゲーム内確認」を表示
- [ ] localStorage は使わず、状態はメモリ内 or エクスポート/インポート方式
      （※artifact制約。Claude Code でローカル運用するなら localStorage 可）

### フェーズ4: 仕上げ
- [ ] ユーザーが手持ち段階を YAML 側に書いておけるオプション対応
- [ ] README（使い方・データ更新方法・出典）

---

## 7. 設計上の注意・ハマりどころ

- **ハルシネーション厳禁**: 倍率・上限が不明なものは捏造せず `null` /
  `unknown` のままにする。ユーザーの明示要望でもある。
- **同名ゲーム混同に注意**: データ収集時、URLドメインを必ず確認。
- **バージョン変動**: 2018年配信の長期運営タイトル。倍率は調整されうる。
  `data_version` と `last_updated` を必ず更新する運用にする。
- **玄武将は別エントリ**: 覚醒版（玄〇〇）は `base_general_id` で覚醒元と
  リンクしつつ、独立エントリとして持つ。
- **計算は段階解決 → スコープ集計 → 上限適用 の順**を厳守。

---

## 8. 添付ファイル

| ファイル | 内容 |
|---|---|
| `HANDOFF.md` | 本ファイル（引き継ぎ仕様書） |
| `generals_sample.yaml` | 武将データ構造サンプル v0.2（張遼・于禁の2体） |

---

## 9. 最初の一手（Claude Code へ）

1. このファイルと `generals_sample.yaml` を読む
2. セクション4「未決事項」をユーザーに確認する
3. 合意が取れたら `schema.md` を書き、フェーズ2のデータ収集に進む
4. データが十分たまったらフェーズ3のシミュレーター実装へ
